class Circle {

	public:
        Circle(): centreX(0.0),centreY(0.0),radius(0.0),sd(0.0),isDefined(false){};
        double centreX;
        double centreY;

        double radius, sd;

        bool isDefined;

};
